import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const ButtonWithIcon = () => {
  return (
    <View>
      <Text>ButtonWithIcon</Text>
    </View>
  );
};

export default ButtonWithIcon;

const styles = StyleSheet.create({});
